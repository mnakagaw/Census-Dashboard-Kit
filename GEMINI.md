# Antigravity / Gemini country dashboard instructions

A request naming a country and asking for its dashboard is an execution request. Treat the country name as sufficient input and complete the work in one Local-project run.

Read and follow `README.md`, `START_HERE.md`, `AGENTS.md`, `docs/COUNTRY_AGENT_WORKFLOW.md`, `docs/COUNTRY_COMPLETION_CONTRACT.md`, `docs/AI_FAILURE_MODE_REVIEW.md` and `templates/ACCEPTANCE.md`. The canonical user prompt is `prompts/ONE_COUNTRY_COMPLETE.md`.

Read `docs/REFERENCE_COUNTRY_CASES.md`, the target's record in `config/jica-priority-source-preflight.json`, and any matching record in `config/americas-verified-source-recipes.json`. Replay a matching Americas recipe before generic search, including its acquisition method, semantic cautions and geography exceptions, but re-acquire and re-hash the current sources. DOM, UGA, LAO and BGD may themselves be requested as fresh targets; refresh and rebuild them instead of returning an old example. Source-address records and historical recipes are starting points, never proof of current-project acquisition or content verification.

`create-country.mjs` is only preparation. Never show its first generated page as the result. Do not stop at command approvals, source discovery, one sample file, a running preview or green checks. Continue through all official catalogue resources, all source tables/fields, nationwide geography, the three usable pages, actual browser interaction, the rendered Word diagnostic, 42 scenarios, twelve-case audit and a `verify-delivery` result of `ready: true`.

When Antigravity requests command access, tell the user the single project-scoped option/button needed and resume immediately after approval. Do not use the confirmation pause as a completion point. Preserve missing/zero/calculated/source-reported distinctions and never fabricate geography, values, legal authority or official status.
