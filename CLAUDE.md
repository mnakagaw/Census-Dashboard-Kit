# Claude Code country dashboard instructions

A request naming a country and asking for its dashboard is an execution request. Treat the country name as sufficient input and complete the work in one run.

Read and follow `README.md`, `START_HERE.md`, `AGENTS.md`, `docs/COUNTRY_AGENT_WORKFLOW.md`, `docs/COUNTRY_COMPLETION_CONTRACT.md`, `docs/AI_FAILURE_MODE_REVIEW.md` and `templates/ACCEPTANCE.md`. The canonical user prompt is `prompts/ONE_COUNTRY_COMPLETE.md`.

Read `config/world-country-area-registry.json` and the target's record in `config/world-source-preflight.json` for every country or area. Read the JICA preflight only as an additional supplement when present, then read `docs/REFERENCE_COUNTRY_CASES.md` and any matching Americas recipe. Replay a matching recipe before generic search, but re-acquire and re-hash current sources. DOM, UGA, LAO and BGD may themselves be fresh targets. Source-address records and historical recipes are starting points, never proof of acquisition or verification. Follow target-specific census systems and national, federal, devolved or autonomous-region planning law rather than assuming one questionnaire census and one national municipal law.

Do not return after cloning, scaffolding, the first API response, one representative local file, a local server, or green unit tests. Close official catalogues resource by resource, inspect every acquired table/field, build and browser-test all three pages and the rendered Word diagnostic, complete the 42 scenarios and twelve-case audit, and require `verify-delivery` to return `ready: true`.

Continue autonomous routine implementation and research. Ask only for genuinely ambiguous country identity or a necessary missing external credential/permission. Preserve missing/zero/calculated/source-reported distinctions and never fabricate geography, values, legal authority or official status.
